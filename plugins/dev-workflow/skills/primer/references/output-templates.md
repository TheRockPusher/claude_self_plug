# Primer Output Templates

## ARCHITECTURE.md Template

```markdown
> Auto-generated by primer skill. Last updated: {YYYY-MM-DD}
> Preserve custom content in `<!-- CUSTOM -->` blocks

# Architecture

## Overview

{Brief description of what the project does and its primary purpose}

## Directory Structure

```tree
project/
├── src/           # {purpose}
├── tests/         # {purpose}
└── ...
```

## Components

### {Component Name}

**Location**: `path/to/component`
**Purpose**: {What it does}

**Key files**:

- `file.py:15-45` - {description}

## Patterns

### {Pattern Name}

**Used for**: {When this pattern applies}
**Example**: `path/to/example.py:100-120`

```python
{Real code example from codebase}
```

## Dependencies

| Package   | Version   | Purpose    |
|-----------|-----------|------------|
| {name}    | {version} | {why used} |

## Entry Points

- **Main**: `src/main.py` - Application entry
- **CLI**: `src/cli.py` - Command line interface
- **API**: `src/api/` - REST endpoints

<!-- CUSTOM -->
{User additions preserved here}
<!-- /CUSTOM -->
```markdown

## CONVENTIONS.md Template

```markdown
> Auto-generated by primer skill. Last updated: {YYYY-MM-DD}
> Preserve custom content in `<!-- CUSTOM -->` blocks

# Conventions

## Naming

| Type | Convention | Example |
|------|------------|---------|
| Files | {snake_case/kebab-case} | `user_service.py` |
| Classes | {PascalCase} | `UserService` |
| Functions | {snake_case/camelCase} | `get_user()` |
| Constants | {UPPER_SNAKE} | `MAX_RETRIES` |

## Imports

**Order**: {stdlib → third-party → local}

```python
{Real import example from codebase}
```

## Error Handling

**Pattern**: {Custom exceptions / Result types / etc.}

```python
{Real error handling example}
```

## Logging

**Framework**: {logging / structlog / etc.}
**Format**: {pattern}

```python
{Real logging example}
```

## Types

**Approach**: {Full typing / Gradual / None}

```python
{Real type annotation example}
```

## Testing

**Framework**: {pytest / jest / etc.}
**Location**: `tests/`
**Naming**: `test_{module}.py`

```python
{Real test example}
```

<!-- CUSTOM -->
{User additions preserved here}
<!-- /CUSTOM -->
```markdown

## CODE-MAP.md Template

```markdown
> Auto-generated by primer skill. Last updated: {YYYY-MM-DD}
> Preserve custom content in `<!-- CUSTOM -->` blocks

# Code Map

## Quick Reference

| Path | Purpose |
|------|---------|
| `src/` | {description} |
| `tests/` | {description} |

## Detailed Map

### src/

#### {subdirectory}/

| File | Purpose | Key exports |
|------|---------|-------------|
| `file.py` | {what it does} | `Class`, `function` |

### tests/

| File | Tests |
|------|-------|
| `test_file.py` | `src/file.py` functionality |

## Key Files

### {filename} (`path/to/file`)

**Purpose**: {What this file does}
**Dependencies**: {What it imports/uses}
**Used by**: {What depends on it}

Key functions:
- `function_name():line` - {description}

<!-- CUSTOM -->
{User additions preserved here}
<!-- /CUSTOM -->
```markdown
